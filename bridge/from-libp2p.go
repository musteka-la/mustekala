package bridge

/*
// consumeFromLibP2PChan is a wrapper to the for loop that processes the
// messages sent to the FromLibP2P channel.
func (b *Bridge) consumeFromLibP2PChan() {
	log.Debug("consuming fromlibp2p channel")

	// easy to read shorthand
	fromLibP2PChan := b.Channels.FromLibP2P

	for {
		// blocks until it gets a message
		msg := <-fromLibP2PChan
		// TODO
		// IMPLEMENT
		log.Debugf("incoming Message FromLibP2P: %v", msg)
	}
}
*/
